export interface IContactMessage{
    id: string;
    mail: string;
    message : string;
    name : string;
}

export interface IProduct{
    category: string;
    description: string;
    imageUrl : string;
    title : string;
}